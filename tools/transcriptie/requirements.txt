# Lokale transcriptie (bevat een eigen audio-decoder via PyAV; geen aparte
# ffmpeg-installatie nodig).
faster-whisper>=1.0.0

# Enkel nodig als [opkuis] mode = claude
anthropic>=0.40.0

# Optioneel: NVIDIA-GPU inschakelen (veel sneller).
# Dubbelklik daarvoor op  gpu-setup.bat  (installeert de regels hieronder).
# device = auto detecteert de GPU dan automatisch.
# nvidia-cublas-cu12
# nvidia-cudnn-cu12
