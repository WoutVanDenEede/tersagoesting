# Lokale transcriptie (bevat een eigen audio-decoder via PyAV; geen aparte
# ffmpeg-installatie nodig).
faster-whisper>=1.0.0

# Enkel nodig als [opkuis] mode = claude
anthropic>=0.40.0

# Optioneel: laat 'device = auto' een NVIDIA-GPU detecteren.
# Installeer torch enkel als je een GPU hebt (zie INSTALLATIE.md).
# torch
